{{#if loading}}
  <div style="width:100%">
    <div style="transform:scale(0.5)">
      {{karma-loading}}
    </div>
  </div>
{{else}}
  {{#if values.length}}
    {{#each values as |value|}}
      <div class="breakdown">
        <div style="display:flex;justify-content:space-between">
          <div>
            <b class="mr-3">
              {{value.shortname}}
            </b>
            <span class="t-light">
              {{#if siteSettings.Show_values_on_proposals}}
                <b>
                  {{value.count}} votes
                </b>
              {{/if}}
            </span>
          </div>
          <div>
            {{#if siteSettings.Show_values_on_proposals}}
              <b>
                {{value.pct}}
              </b>
            {{/if}}
          </div>
        </div>
        <div
          class="breakdown-progress"
          style="max-height:100%;width:100%;overflow:hidden"
        >
          {{#if (eq value.shortname "Others")}}
            {{karma-tooltip label=value.name}}
          {{/if}}
          {{#if siteSettings.Show_values_on_proposals}}
            <div
              class="breakdown-progress-fill"
              style={{html-safe (concat "width:" value.fillPct)}}
            >
            </div>
          {{/if}}
        </div>
      </div>
    {{/each}}
  {{/if}}
{{/if}}
